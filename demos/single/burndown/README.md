# Burndown demo

This directory contains a ready-made local board for trying the burndown report.
Run the command from this directory:

```bash
cargo run --manifest-path ../../../Cargo.toml -- sprint burndown S-1
```

The demo data represents a closed Sprint with estimated and completed items.
It is intentionally local and can be changed freely without requiring a server.
