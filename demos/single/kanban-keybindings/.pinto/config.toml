columns = [
    "todo",
    "in-progress",
    "review",
    "done",
]
done_column = "done"

[project]
name = "kanban-keybindings"
key = "T"

[tui]
confirm_quit = true
hidden_columns = ["todo"]

[storage]
backend = "file"

[wip]
enabled = true
