+++
id = "P-76"
title = "Move CLI-usage errors out of the domain error enum"
status = "todo"
rank = "hx"
created = "2026-07-29T12:16:21.312915Z"
updated = "2026-07-29T12:16:21.330477Z"
+++

# **Summary**

The domain error enum carries CLI-shaped usage errors (the requires-a-Sprint-ID-or-nested-operation cases that point at --help) which describe argument-parsing outcomes rather than domain failures. Relocate them to the CLI layer so the domain error type stays independent of CLI input shape and testable in isolation.

# **Acceptance Criteria**

- [ ] Command-usage error variants no longer live in the domain error enum
- [ ] The sprint retro and sprint review no-argument guidance is produced at the CLI layer
- [ ] Domain error tests no longer reference CLI-usage variants
- [ ] User-facing behavior and messages for the no-argument case stay unchanged
