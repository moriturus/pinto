+++
id = "P-62"
title = "Route Book Deployments by Branch and Version"
status = "done"
rank = "28or"
start_at = "2026-07-28T08:36:52.058593Z"
done_at = "2026-07-28T08:36:52.058593Z"
created = "2026-07-27T17:06:32.957457Z"
updated = "2026-07-28T08:36:52.058593Z"
+++

# **Summary**

Configure Book deployments to use destinations determined by `main`, `develop`, and the release version, with `main` always representing the latest version.

# **Implementation plan**

1. Define the Pages destinations as a root redirect to `/latest/` for the stable `main` Book, `/develop/` for the development Book, and `/<version>/` for every semantic-version release tag.
2. Use the existing configuration and documentation checks, then validate the route-building script and generated artifact without adding a dedicated P-62 test.
3. Build one Pages artifact from the fetched `main`, `develop`, and release-tag refs so stable, development, and historical version URLs remain available together.
4. Update the Pages workflow and concise repository documentation, then verify the generated routes and the complete project quality gate.

# **Acceptance Criteria**

- [x] Book deployments from `main`, `develop`, and versioned releases resolve to their intended destinations.
- [x] The root deployment redirects to `/latest/`, and `/latest/` contains the same Book generated from `main`.
- [x] Different versioned deployments remain addressable by their version.

# **Verification**

- [x] `bash scripts/build-book-deployment.sh` generated the root redirect, `latest`, `develop`, and all local semantic-version routes.
- [x] The root `index.html` contains both the static `latest/` redirect and a fallback link, while Book assets exist only under routed directories.
- [x] `bash -n scripts/build-book-deployment.sh`, `git fetch --force --dry-run origin main develop --tags`, and the existing documentation/configuration tests passed.
- [x] `mise run check` passed all tests, Clippy with warnings denied, Rust documentation, mdBook, doctor, and formatting.
